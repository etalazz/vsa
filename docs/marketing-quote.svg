<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 1200 180" role="img" aria-label="Under a 50 % churn workload (equal + and − updates), the Vector–Scalar Accumulator (VSA) collapsed 200 000 logical operations into a single commit every ~43 ms — capable of processing over 4.6 million logical events per second (on a single thread)">
  <defs>
    <style>
      .quote { fill: #007ec6; font-family: -apple-system,BlinkMacSystemFont,'Segoe UI','Helvetica Neue',Arial,'Noto Sans','Liberation Sans',sans-serif; font-style: italic; }
      .line1 { font-size: 24px; }
      .line2 { font-size: 20px; }
    </style>
  </defs>
  <rect x="0" y="0" width="1200" height="180" fill="transparent"/>
  <g text-anchor="middle">
    <text class="quote line1" x="600" y="78">“Under a 50 % churn workload (equal + and − updates), the Vector–Scalar Accumulator (VSA)</text>
    <text class="quote line1" x="600" y="108">collapsed 200 000 logical operations into a single commit every ~43 ms”</text>
    <text class="quote line2" x="600" y="142">— capable of processing over 4.6 million logical events per second (on a single thread)</text>
  </g>
</svg>
